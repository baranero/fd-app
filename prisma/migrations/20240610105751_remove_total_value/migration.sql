/*
  Warnings:

  - You are about to drop the column `totalValue` on the `warehouse` table. All the data in the column will be lost.

*/
-- AlterTable
ALTER TABLE "warehouse" DROP COLUMN "totalValue";
